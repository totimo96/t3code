# Ralph Progress Log

Started: 2026-05-18

## Codebase Patterns

- Project completion checks are `bun fmt`, `bun lint`, and `bun typecheck`.
- Do not run `bun test`; use `bun run test` when tests are needed.

---

## 2026-05-19 09:55 +03:00 - US-008

- Added Design Variant candidate generation from the selected Design Target on the Design Canvas.
- Generates three complete Standalone Design Document candidates by default, each carrying parent artifact version and target anchor metadata.
- Added variant preview iframes using the same sandboxed rendering path.
- Added Accept Design Variant, which promotes the chosen candidate through the existing `design.artifact.update` orchestration command and does not write project files.
- Files changed:
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/ChatView.logic.ts`
  - `apps/web/src/components/ChatView.logic.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/components/ChatView.logic.test.ts -t "design variants|coding handoff"`: passed
- Learnings for future iterations:
  - Variant candidates can use the existing Design Artifact update path for acceptance; a separate persistence model is not needed until variants must survive reload before acceptance.

---

## 2026-05-19 09:55 +03:00 - US-009

- Added Apply Design Artifact as a Coding Handoff from the Design Canvas.
- Applying creates a new Coding Workspace thread and starts it with a prompt containing the current Design Artifact, artifact version metadata, and Design Brief when present.
- The handoff does not write project files directly; normal Coding Thread execution owns implementation diffs.
- Files changed:
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/ChatView.logic.ts`
  - `apps/web/src/components/ChatView.logic.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/components/ChatView.logic.test.ts -t "design variants|coding handoff"`: passed
- Learnings for future iterations:
  - Coding Handoff should remain prompt-first and route to a Coding Thread instead of sharing Design Thread state or touching files directly.

---

## 2026-05-19 09:32 +03:00 - US-007

- Added Click Targeting and Box Targeting controls to the Design Canvas.
- Injected a controlled iframe bridge into Design Artifact `srcDoc` while keeping the existing `allow-scripts` sandbox without `allow-same-origin`.
- Captured Design Target anchors with render geometry and DOM context from sandbox messages.
- Built Target Prompt Context that includes artifact version, full Standalone Design Document HTML, anchor geometry, DOM/text context, visual-crop status, and Design Brief when present.
- Inserts the generated Target Prompt Context into the existing composer for the next design prompt.
- Files changed:
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/ChatView.logic.ts`
  - `apps/web/src/components/ChatView.logic.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/components/ChatView.logic.test.ts -t "design target"`: passed
- Learnings for future iterations:
  - Keep Design Canvas bridge behavior in `ChatView.logic.ts` so iframe props and prompt-context formatting are unit-testable without mounting the full route.
  - Target Prompt Context should include the complete Standalone Design Document, not only the artifact version, because variant generation must preserve the whole design.

---

## 2026-05-19 09:21 +03:00 - US-006

- Added an explicit chat-code action for complete standalone HTML documents in Design Workspace.
- Routed that action through `design.artifact.update`, preserving versioned Design Artifact metadata.
- Added structured provider action ingestion for `design.artifact.create`/`design.artifact.update` and matching tool names without auto-persisting arbitrary assistant HTML.
- Added focused tests for HTML document detection, the manual browser action, structured provider creation, and arbitrary HTML non-creation.
- Files changed:
  - `apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts`
  - `apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.test.ts`
  - `apps/web/src/components/ChatMarkdown.tsx`
  - `apps/web/src/components/ChatMarkdown.browser.tsx`
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/chat/MessagesTimeline.tsx`
  - `apps/web/src/lib/designArtifacts.ts`
  - `apps/web/src/lib/designArtifacts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/lib/designArtifacts.test.ts`: passed
  - `bun run --cwd apps/web test:browser src/components/ChatMarkdown.browser.tsx -t artifact`: passed after installing Playwright Chromium with the repo script
  - `bun run --cwd apps/server test src/orchestration/Layers/ProviderRuntimeIngestion.test.ts -t "design artifact"`: passed
- Learnings for future iterations:
  - Browser component tests need `bun run --cwd apps/web test:browser ...`; the normal web test include does not pick up `*.browser.tsx`.
  - Assistant text deltas in provider ingestion tests require the matching `item.completed` event before finalized messages appear in projections.

---

## 2026-05-18 17:22 +03:00 - US-001

- Implemented Thread Workspace Kind across contracts, server projections, persistence, and the web sidebar.
- Added a persisted `workspace_kind` projection column with legacy fallback to Coding Workspace.
- Added a Code/Design Workspace Switch in the sidebar and workspace-filtered sidebar selectors.
- Files changed:
  - `.gitignore`
  - `packages/contracts/src/orchestration.ts`
  - `apps/server/src/orchestration/*`
  - `apps/server/src/persistence/*`
  - `apps/web/src/components/Sidebar.tsx`
  - `apps/web/src/store.ts`
  - `apps/web/src/types.ts`
  - `apps/web/src/environmentGrouping.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/environmentGrouping.test.ts`: passed
  - `bun run --cwd apps/server test src/persistence/Layers/ProjectionRepositories.test.ts`: passed
- Learnings for future iterations:
  - Projection migrations must be statically imported and registered in `apps/server/src/persistence/Migrations.ts`.
  - Root `bun run test <file>` is not a valid targeted test entrypoint because the root script delegates to Turbo; run package-local test scripts with `bun run --cwd <package> test <file>`.

---

## 2026-05-18 21:39 +03:00 - US-002

- Added a direct Design Route at `/design/$environmentId/$threadId`.
- Reused `ChatView` for the first Design Thread surface with a design-specific empty state.
- Added route-aware thread navigation so Design Workspace threads open through the Design Route from Sidebar, Command Palette, and fallback thread actions.
- Added a route helper for workspace-kind-specific thread paths and covered it in `threadRoutes.test.ts`.
- Files changed:
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/CommandPalette.logic.ts`
  - `apps/web/src/components/CommandPalette.tsx`
  - `apps/web/src/components/Sidebar.tsx`
  - `apps/web/src/hooks/useThreadActions.ts`
  - `apps/web/src/routes/design.$environmentId.$threadId.tsx`
  - `apps/web/src/routeTree.gen.ts`
  - `apps/web/src/threadRoutes.ts`
  - `apps/web/src/threadRoutes.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/web test src/threadRoutes.test.ts`: passed
- Learnings for future iterations:
  - Use `threadRoutePathForWorkspaceKind` when navigating to an existing thread from generic UI surfaces.
  - Ralph/Codex currently tends to repeat large diffs; keep running one iteration at a time and stabilize the slice locally if it loops.

---

## 2026-05-19 00:38 +03:00 - US-005

- Added Design Asset contracts (id brand, asset schema, command, event, payload, thread field).
- Persisted assets server-side via `projection_design_assets` (migration 034) plus a projector that writes binary bytes from the base64-encoded event payload into the new `design-assets/` state dir.
- Added a `/design-assets/<assetId>` HTTP route mounted alongside `/attachments`, authenticated and limited to single-segment ids — no traversal, no nested paths.
- Threaded `designAssets` through the snapshot/command/thread-detail loaders, the web store slice, and the thread derivation cache.
- Added a `DesignAssetsSection` (file picker + copy-URL list) to the existing Design Brief panel so users can upload images and copy the controlled URL that artifacts should reference.
- Helpers `extractImageSourcesFromHtml` and `isControlledDesignAssetUrl` document the controlled-asset URL contract for future artifact generation/validation.
- Files changed:
  - `packages/contracts/src/baseSchemas.ts`
  - `packages/contracts/src/orchestration.ts`
  - `apps/server/src/config.ts`
  - `apps/server/src/designAssetStore.ts` + `apps/server/src/designAssetStore.test.ts`
  - `apps/server/src/http.ts`
  - `apps/server/src/server.ts`
  - `apps/server/src/server.test.ts`
  - `apps/server/src/orchestration/Schemas.ts`
  - `apps/server/src/orchestration/decider.ts`
  - `apps/server/src/orchestration/projector.ts`
  - `apps/server/src/orchestration/projector.test.ts`
  - `apps/server/src/orchestration/commandInvariants.test.ts`
  - `apps/server/src/orchestration/Layers/OrchestrationEngine.test.ts`
  - `apps/server/src/orchestration/Layers/ProjectionPipeline.ts`
  - `apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts`
  - `apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.test.ts`
  - `apps/server/src/persistence/Services/ProjectionDesignAssets.ts`
  - `apps/server/src/persistence/Layers/ProjectionDesignAssets.ts`
  - `apps/server/src/persistence/Migrations.ts`
  - `apps/server/src/persistence/Migrations/034_ProjectionDesignAssets.ts`
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/ChatView.logic.ts`
  - `apps/web/src/components/ChatView.logic.test.ts`
  - `apps/web/src/components/ChatView.browser.tsx`
  - `apps/web/src/components/KeybindingsToast.browser.tsx`
  - `apps/web/src/lib/utils.ts`
  - `apps/web/src/lib/designAssets.ts` + `apps/web/src/lib/designAssets.test.ts`
  - `apps/web/src/store.ts`
  - `apps/web/src/store.test.ts`
  - `apps/web/src/threadDerivation.ts`
  - `apps/web/src/types.ts`
  - `apps/web/src/environmentGrouping.test.ts`
  - `docs/design-workspace-issues/005-design-assets-images.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed (9 pre-existing warnings, no new ones)
  - `bun typecheck`: passed
  - `bun run --cwd apps/server test src/orchestration/projector.test.ts src/designAssetStore.test.ts`: 23/23 passed
  - `bun run --cwd apps/server test src/server.test.ts -t "design asset"`: 4/4 passed
  - `bun run --cwd apps/web test src/store.test.ts src/components/ChatView.logic.test.ts src/environmentGrouping.test.ts src/lib/designAssets.test.ts`: 71/71 passed
- Learnings for future iterations:
  - The web `iframe-missing-sandbox` lint pattern still applies — sandbox attributes must be literal JSX, even on assets-bearing iframes.
  - When `OrchestrationThread` gains a non-optional projection field (`Schema.Array(...)` with a decoding default), every server-side fixture that builds a thread literal needs the new key — there's no quick way to default the property at the type level.
  - Adding a new projection that writes to disk needs to map `PlatformError` to `ProjectionRepositoryError`; otherwise the projector signature widens and breaks the central `ProjectorDefinition.apply` constraint.

---

## 2026-05-18 23:17 +03:00 - US-004

- Added Design Artifact contracts (event, payload, command, thread field) plus a sandboxed Design Canvas.
- Added orchestration decider + projector handling for `design.artifact-updated`.
- Added a persisted `projection_design_artifacts` table, repository, layer, and pipeline projector, and wired the new rows through every projection snapshot/command/thread-detail loader.
- Added a `DesignCanvas` web component that renders the current artifact through an `<iframe srcdoc sandbox="allow-scripts" referrerpolicy="no-referrer">` so generated HTML never touches the T3 Code DOM.
- Files changed:
  - `packages/contracts/src/orchestration.ts`
  - `apps/server/src/orchestration/Schemas.ts`
  - `apps/server/src/orchestration/decider.ts`
  - `apps/server/src/orchestration/projector.ts`
  - `apps/server/src/orchestration/projector.test.ts`
  - `apps/server/src/orchestration/Layers/ProjectionPipeline.ts`
  - `apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts`
  - `apps/server/src/persistence/Services/ProjectionDesignArtifacts.ts`
  - `apps/server/src/persistence/Layers/ProjectionDesignArtifacts.ts`
  - `apps/server/src/persistence/Migrations.ts`
  - `apps/server/src/persistence/Migrations/033_ProjectionDesignArtifacts.ts`
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/components/ChatView.logic.ts`
  - `apps/web/src/components/ChatView.logic.test.ts`
  - `apps/web/src/store.ts`
  - `apps/web/src/store.test.ts`
  - `apps/web/src/threadDerivation.ts`
  - `apps/web/src/types.ts`
  - `apps/web/src/environmentGrouping.test.ts`
  - `docs/design-workspace-issues/004-artifact-store-sandboxed-canvas.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with pre-existing warnings only
  - `bun typecheck`: passed
  - `bun run --cwd apps/server test src/orchestration/projector.test.ts`: passed (13/13)
  - `bun run --cwd apps/web test src/store.test.ts src/components/ChatView.logic.test.ts src/environmentGrouping.test.ts`: passed (67/67)
- Learnings for future iterations:
  - The web `iframe-missing-sandbox` oxlint rule only inspects literal JSX attributes; spread props won't satisfy it, so keep the sandbox attribute inline on the element.
  - Adding a new projector requires four touch points: the pipeline (apply + registration + layer merge), the snapshot query (snapshot/command-read-model/thread-detail), `REQUIRED_SNAPSHOT_PROJECTORS`, and the migration list.

---

## 2026-05-18 22:31 +03:00 - US-003

- Added a versioned Design Brief command/event path through orchestration contracts, decider, projector, and projection snapshots.
- Added a persisted `projection_design_briefs` table and projection repository.
- Added Design Brief detail state in the web store and a Design Workspace side panel for viewing/editing the brief.
- Future Design Workspace sends include the current Design Brief as prompt context without changing existing messages/artifacts.
- Files changed:
  - `packages/contracts/src/orchestration.ts`
  - `apps/server/src/orchestration/*`
  - `apps/server/src/persistence/*`
  - `apps/web/src/components/ChatView.tsx`
  - `apps/web/src/store.ts`
  - `apps/web/src/threadDerivation.ts`
  - `apps/web/src/types.ts`
  - `apps/server/src/orchestration/projector.test.ts`
  - `apps/web/src/store.test.ts`
- Checks run and result:
  - `bun fmt`: passed
  - `bun lint`: passed with existing warnings
  - `bun typecheck`: passed
  - `bun run --cwd apps/server test src/orchestration/projector.test.ts`: passed
  - `bun run --cwd apps/web test src/store.test.ts`: passed
- Learnings for future iterations:
  - Detail-only thread state needs a dedicated web-store slice when it is not part of thread shell state.
  - Projection snapshot sequence must include new required projectors when snapshot correctness depends on them.

---
