# Design Workspace Uses Versioned Artifacts

Design work in T3 Code uses dedicated Design Threads backed by versioned Design Artifacts rather than treating project files or normal coding threads as the live design source. This keeps canvas rendering, target selection, variant comparison, restore, and reconnect behavior predictable while still allowing accepted artifacts to be explicitly applied to project files later.
